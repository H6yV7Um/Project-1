import {ACTIONS} from '../actions/DingdingAction'

const initialState = {

}

/**
 * Reducer
 */
const reducer = (state = initialState, action) => {
    switch (action.type) {

    }

    return {...state}
}
export default reducer